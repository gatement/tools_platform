{application, gen_tcp_server,
 [{description, "A generic tcp server behaviour"},
  {vsn, "0.1.0"},
  {registered, []},
  {applications, [
			  	kernel, 
			  	stdlib, 
			  	core]},
  {env, [
    {connection_idle_timout, 20000}, % in milliseconds
    {heartbeat_checking_interval, 30000}, % in milliseconds
    {init_worker_count, 2}
  ]}
 ]}.
