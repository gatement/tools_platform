[
{client, [
    {server_host, "localhost"},
    {server_port, 10000},
    
    {run_mode, once}, % once/live

    {reconnect_wait_time, 10000},
    {data_sending_interval, 10000},

    {client_creating_interval, 1000},
    {client_count_init, 1},
    {client_count_total, 1}
]}
].